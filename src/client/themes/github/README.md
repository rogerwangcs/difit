# GitHub syntax theme

Syntax highlighting for diff code surfaces uses Shiki's built-in `github-dark-default` theme from [@shikijs/themes](https://github.com/shikijs/shiki) — MIT License.

UI chrome remains Nord via `nordThemeAdapter.ts`. Only the code window uses GitHub token colors and diff blob backgrounds.
